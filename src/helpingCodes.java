//lesson8 recording
/*public class helpingCodes {
    //in one of classes we can define static parameter
    //public class Student
    public static int year;
    public static String country;
    //we can use static block for adding values for static parameters
    //static{sout("year value is set"); country="yerevan, sout ="our locasion islocation"+country; }
    //psvm-um Stutent stut = new Student(
    //sout stut.year;



    //count of guests
    //public static int count;
    //public Student(){count++;}
    //mainum exac studentneri qanaky kta, ete stut.count kam Student.count print anenq


    //strong password code
    //psvm String password ="as56h";
    //f(password); //print ani strong a passwordy te che

    //private static void f(String password){
    if(password.length()<=8){
        System.out.println("not strong");
        return;
    }
    int countOfNumbers=0;
    int getCountOfCapitalLetters=0;
    for(int i=0;i<password.length();i++){
        char c = password.charAt(i);
        if(Character.isDigit()){
            countOfNumbers++;
        }else{
            if(Character.isUpperCase()){
                getCountOfCapitalLetters++;
            }
        }
    }
    if(countOfNumbers>=4 && getCountOfCapitalLetters>=3){
        System.out.println("password is strong");
    }else{
        System.out.println("not strong");
    }
}
*/

//// public static final String LOCATION = "Armenia";
// LOCATION popoxakani arjeqy chi poxvum el!!!!